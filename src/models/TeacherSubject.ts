export interface TeacherSubject {
  id: number;
  code: string;
  name: string;
  correlatives: any[];
}

export interface TeacherSubjectCamelCase {
  id: number;
  code: string;
  name: string;
  correlatives: any[];
}
